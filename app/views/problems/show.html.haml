%h2= @problem.title
%div
  = @problem.prompt

%h4 Solutions
%table
  %thead
    %tr
      %th Language
      %th Answer
      %th Run Time
  %tbody
    - @problem.solutions.each do |s|
      %tr
        %td= link_to(s.language, User.editor_url(s.path))
        %td= s.answer
        %td= s.runtime
  